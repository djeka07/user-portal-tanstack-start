import { dict } from '~/locales/en';
import { dict as svDict } from '~/locales/sv';

export const supportedLngs = ['sv', 'en'];
export const fallbackLng = 'en';
export const defaultNS = 'common';
export const resources = {
  en: dict,
  sv: svDict,
};
